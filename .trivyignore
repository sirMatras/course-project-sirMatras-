# Trivy ignore политики для ложных срабатываний
# Формат: CVE-ID или компонент:версия:CVE-ID

# Игнорируем уязвимости в Python runtime, если они не критичны для нашего использования
# (только если не критичные для безопасности приложения)

# Пример (раскомментировать при необходимости):
# CVE-2024-XXXXX  # Причина игнорирования

